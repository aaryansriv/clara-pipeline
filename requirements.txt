# Clara Pipeline — Dependencies
# All tools are free-tier / zero-cost

# Core — LLM extraction
groq>=0.4.0

# Optional — Google Sheets integration
# google-api-python-client>=2.0.0
# google-auth>=2.0.0

# Optional — Trello integration
# requests>=2.28.0   (usually pre-installed)
